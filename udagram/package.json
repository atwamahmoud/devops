{
    "scripts": {
        "install:fe": "cd udagram-frontend && npm i",
        "install:api": "cd udagram-api && npm i",
        "install": "npm-run-all install:*",
        "build:fe": "cd udagram-frontend && npm run build",
        "build:api": "cd udagram-api && npm run build",
        "build": "npm-run-all build:*",
        "test:fe": "cd udagram-frontend && npm run test -- --watch=false",
        "test:api": "cd udagram-api && npm run test",
        "test": "npm run test:fe",
        "debloy:fe": "aws s3 cp udagram-frontend/www/ s3://udagram-web-hosting/ --recursive",
        "debloy:api": "",
        "deploy": "npm-run-all deploy:*"
    },
    "devDependencies": {
        "cross-env": "^7.0.3",
        "npm-run-all": "^4.1.5"
    }
}
